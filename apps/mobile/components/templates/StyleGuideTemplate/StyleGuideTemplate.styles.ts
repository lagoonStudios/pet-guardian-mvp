import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  safeArea: {
    flex: 1,
  },
  content: {
    padding: 20,
    gap: 16,
  },
  section: {
    gap: 12,
  },
  surfaceSample: {
    padding: 16,
    borderRadius: 16,
    gap: 8,
  },
  cardIcon: {
    marginLeft: 16,
    marginTop: 16,
  },
});
